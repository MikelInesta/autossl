import React from "react";

const HomePage: React.FC = () => {

    /*
        Maybe only a sidebar with links to the different pages
        and the changing content in the main area
    */

  return (
    <div>
      <h1>AutoSSL</h1>
      <SidebarMenu />
    </div>
  );
};

export default HomePage;
